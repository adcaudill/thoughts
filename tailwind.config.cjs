module.exports = {
    content: ['./index.html', './src/**/*.{ts,tsx,js,jsx}'],
    // Follow the user's OS/browser preference for dark mode by default.
    // Components can still use `dark:` variants if explicit overrides are desired.
    darkMode: 'media',
    theme: {
        extend: {},
    },
    plugins: [],
}
